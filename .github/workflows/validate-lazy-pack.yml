name: Validate lazy pack

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Validate chapters and skills
        shell: pwsh
        run: ./scripts/validate-lazy-pack.ps1
